package com.company;

import java.io.File;
import java.util.ArrayList;

public class TournamentSystem
{
    /*
    private TextUI textUI = new TextUI();
    private FileIO fileIO= new FileIO();
    ArrayList<Registration> registration = new ArrayList<>();
    Board board;

    public static void runTournament() {
    }


    private void gameSetup() {
        //**********************
        // Load af spiller data
        // **********************
        ArrayList<String> data;
        data =  fileIO.inputData();
        if(data == null){

            data = textUI.getPlayerNames("Skriv spillernavn. Tast Q for at quitte");
        }
        this.createPlayers(data);
        //**********************
        // Load af felt data
        // **********************
        String[] fieldData = fileIO.readData();
        board = new Board(fieldData);

    }

    private void createPlayers(ArrayList<String> data){

        for (String s : data) {
            String[] values = s.split(", "); //split arrayet så vi får adskildt de to værdier

            Registration p = new Registration(); // brug de to værdier til at lave en ny Player instans

            Registration.add(p);                            // tilføj Player instansen til array'et af spillere

        }

    }*/


}